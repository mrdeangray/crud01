// import logo from './logo.svg';
import './App.css';
import { Routes, Route } from 'react-router-dom';

function App() {
  return (
    <div className="App">
        <Routes>
          {/* <Route exact path="/" element={<Todos />} />
          <Route exact path="/new" element={<AddTodo />} />
          <Route exact path="/edit/:todo" element={<TodoEdit />} /> */}
        </Routes>
    </div>
  );
}

export default App;
